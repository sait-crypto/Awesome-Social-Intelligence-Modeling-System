# Engineering

Implementation code, configuration, tests, build artifacts, documentation, assets, backups, and the two paper databases live here. Run repository commands from the repository root.

- Generate lists: `python engineering/src/convert.py`
- Open submission GUI: `python submit.py`
- Run tests: `$env:PYTHONPATH='engineering'; python -m unittest discover -s engineering/tests`
